/**
 * @file pageSizeStyle.ts
 * @author zhouminghui
 * @description 获取屏幕宽高初始化浮层
*/

export const pageSizeStyle = {
    width: window.innerHeight || document.documentElement.clientHeight ,
    height: window.innerWidth || document.documentElement.clientWidth
};

